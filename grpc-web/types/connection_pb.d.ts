import * as jspb from 'google-protobuf'



export enum Connectivity { 
  NOT_APPLICABLE = 0,
  DISCONNECTED = 1,
  CONNECTED = 2,
}
export enum CommStatus { 
  COMM_UNKNOWN = 0,
  COMM_SUCCESS = 1,
  COMM_FAILURE = 2,
}
